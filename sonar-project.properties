sonar.projectKey=OneBusAway_hooks
sonar.organization=onebusaway

# Test files routinely reuse the same HTTP-request boilerplate (NewRequest →
# Do → ReadAll → Close); SonarCloud's CPD treats that as duplication and
# blocks the merge gate. Production code is still subject to CPD.
sonar.cpd.exclusions=**/*_test.go
