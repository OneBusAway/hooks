// Minimal htmx shim — full progressive-enhancement library is unnecessary for
// the small handful of hx-* attributes we use. Native form submission + page
// reload covers the same behaviors.
(function(){})();
